rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 単一ユーザー専用アプリ。ログイン済みの本人のみ全データにアクセス可能。
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
